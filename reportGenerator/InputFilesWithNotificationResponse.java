package reportGenerator;

import java.io.File;

public class InputFilesWithNotificationResponse extends InputFiles {

	@Override
	public void noCommissioningReport() {
		/**
		 * This method invoke message with OK button, so automated testing is disabled. Here we set method to do nothing so that automatic testing is
		 * enable.
		 */
	}

	@Override
	protected void isResultValid(String result, String parameter) {
		/**
		 * This method invoke message with OK button, so automated testing is disabled. Here we set method to do nothing so that automatic testing is
		 * enable.
		 */
	}
}
